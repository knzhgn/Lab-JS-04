// module3.js — Configuration Module
// Centralizes all config constants — eliminates hardcoded values

'use strict';

const TAX_RATE = 0.08;
const CURRENCY = 'USD';
const MAX_CART_ITEMS = 50;

module.exports = { TAX_RATE, CURRENCY, MAX_CART_ITEMS };
